import React, { Component } from 'react';
import { expression } from '@babel/template';

class Navbar extends Component {
    render() {
        return (
            <div className="navbar">

                <div className="container">
                    <button>Show Latest news</button>
                </div>

            </div>
        )
    }
}

export default Navbar;